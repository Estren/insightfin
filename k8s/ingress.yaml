apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: insightfin-ingress
  namespace: insightfin
  labels:
    app.kubernetes.io/part-of: insightfin
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: core-api
                port:
                  number: 8080
          - path: /q
            pathType: Prefix
            backend:
              service:
                name: core-api
                port:
                  number: 8080
