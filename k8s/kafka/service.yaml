apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: orizon
  labels:
    app.kubernetes.io/name: kafka
    app.kubernetes.io/part-of: orizon
    app.kubernetes.io/component: messaging
spec:
  selector:
    app.kubernetes.io/name: kafka
  ports:
    - name: client
      port: 9092
      targetPort: 9092
