# IMPORTANT: Replace all placeholder values before applying to any environment.
# Use `kubectl create secret generic` or a secrets manager (Vault, Azure Key Vault) in production.
apiVersion: v1
kind: Secret
metadata:
  name: insightfin-secrets
  namespace: insightfin
  labels:
    app.kubernetes.io/part-of: insightfin
type: Opaque
stringData:
  DB_PASSWORD: 'change-me'
  JWT_SECRET: 'change-me-min-32-chars-long-secret-key'
  AZURE_OPENAI_ENDPOINT: 'https://<your-resource>.openai.azure.com/'
  AZURE_OPENAI_KEY: 'change-me'
